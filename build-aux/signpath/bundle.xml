<!--
The bundle's version resource comes from WiX's Bundle/@Version (the
four-part BundleVersion), not from TELEPRESENCE_VERSION, so this
configuration carries no product-version restriction.
-->
<artifact-configuration xmlns="http://signpath.io/artifact-configuration/v1">
  <pe-file>
    <authenticode-sign />
  </pe-file>
</artifact-configuration>
