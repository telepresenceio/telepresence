# This Gemfile is used to configure the statically built
# docs in the docs/ directory. It is *not* in the docs/
# directory because Netlify does not handle the subdirectory
# correctly.
source 'https://rubygems.org'
gem "jekyll"
gem "jekyll-redirect-from"
