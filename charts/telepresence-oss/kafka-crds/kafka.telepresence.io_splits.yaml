---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
    helm.sh/resource-policy: keep
  name: splits.kafka.telepresence.io
spec:
  group: kafka.telepresence.io
  names:
    kind: KafkaSplit
    listKind: KafkaSplitList
    plural: splits
    shortNames:
    - ksplit
    singular: split
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.desiredState
      name: State
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: KafkaSplit is one standing Kafka consumer-group split.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: KafkaSplitSpec declares one original Kafka consumer group.
            properties:
              application:
                description: |-
                  KafkaApplicationSpec describes the selected container's configuration
                  adapter.
                properties:
                  groupEnv:
                    type: string
                  isolationLevelEnv:
                    type: string
                  shadowCredentials:
                    additionalProperties:
                      description: ValueSource is a literal or one key in a Secret.
                      properties:
                        secretKeyRef:
                          description: SecretKeySelector selects a key of a Secret.
                          properties:
                            key:
                              description: The key of the secret to select from.  Must
                                be a valid secret key.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the Secret or its key must
                                be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                        value:
                          type: string
                      type: object
                    type: object
                  topicBindings:
                    items:
                      description: KafkaTopicEnvBinding maps one source topic to an
                        application variable.
                      properties:
                        env:
                          type: string
                        source:
                          type: string
                      required:
                      - env
                      - source
                      type: object
                    type: array
                  topicEnv:
                    type: string
                  topicSeparator:
                    type: string
                  transactionalIDEnv:
                    type: string
                required:
                - groupEnv
                - isolationLevelEnv
                type: object
              connection:
                description: KafkaConnectionSpec contains provider-only broker connection
                  settings.
                properties:
                  bootstrapServers:
                    items:
                      type: string
                    minItems: 1
                    type: array
                  sasl:
                    description: KafkaSASLSpec configures one Kafka SASL mechanism.
                    properties:
                      aws:
                        description: KafkaAWSSpec configures AWS MSK IAM authentication.
                        properties:
                          region:
                            type: string
                        required:
                        - region
                        type: object
                      kerberos:
                        description: KafkaKerberosSpec configures SASL/GSSAPI.
                        properties:
                          config:
                            description: SecretKeySelector selects a key of a Secret.
                            properties:
                              key:
                                description: The key of the secret to select from.  Must
                                  be a valid secret key.
                                type: string
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                              optional:
                                description: Specify whether the Secret or its key
                                  must be defined
                                type: boolean
                            required:
                            - key
                            type: object
                            x-kubernetes-map-type: atomic
                          keytab:
                            description: SecretKeySelector selects a key of a Secret.
                            properties:
                              key:
                                description: The key of the secret to select from.  Must
                                  be a valid secret key.
                                type: string
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                              optional:
                                description: Specify whether the Secret or its key
                                  must be defined
                                type: boolean
                            required:
                            - key
                            type: object
                            x-kubernetes-map-type: atomic
                          password:
                            description: ValueSource is a literal or one key in a
                              Secret.
                            properties:
                              secretKeyRef:
                                description: SecretKeySelector selects a key of a
                                  Secret.
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              value:
                                type: string
                            type: object
                          realm:
                            type: string
                          serviceName:
                            type: string
                          username:
                            description: ValueSource is a literal or one key in a
                              Secret.
                            properties:
                              secretKeyRef:
                                description: SecretKeySelector selects a key of a
                                  Secret.
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              value:
                                type: string
                            type: object
                        required:
                        - config
                        - realm
                        - serviceName
                        - username
                        type: object
                      mechanism:
                        enum:
                        - PLAIN
                        - SCRAM-SHA-256
                        - SCRAM-SHA-512
                        - OAUTHBEARER
                        - GSSAPI
                        - AWS_MSK_IAM
                        type: string
                      oauth:
                        description: KafkaOAuthSpec configures an OAUTHBEARER client-credentials
                          flow.
                        properties:
                          clientID:
                            description: ValueSource is a literal or one key in a
                              Secret.
                            properties:
                              secretKeyRef:
                                description: SecretKeySelector selects a key of a
                                  Secret.
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              value:
                                type: string
                            type: object
                          clientSecret:
                            description: ValueSource is a literal or one key in a
                              Secret.
                            properties:
                              secretKeyRef:
                                description: SecretKeySelector selects a key of a
                                  Secret.
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              value:
                                type: string
                            type: object
                          scopes:
                            items:
                              type: string
                            type: array
                          tokenURL:
                            type: string
                        required:
                        - clientID
                        - clientSecret
                        - tokenURL
                        type: object
                      password:
                        description: ValueSource is a literal or one key in a Secret.
                        properties:
                          secretKeyRef:
                            description: SecretKeySelector selects a key of a Secret.
                            properties:
                              key:
                                description: The key of the secret to select from.  Must
                                  be a valid secret key.
                                type: string
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                              optional:
                                description: Specify whether the Secret or its key
                                  must be defined
                                type: boolean
                            required:
                            - key
                            type: object
                            x-kubernetes-map-type: atomic
                          value:
                            type: string
                        type: object
                      username:
                        description: ValueSource is a literal or one key in a Secret.
                        properties:
                          secretKeyRef:
                            description: SecretKeySelector selects a key of a Secret.
                            properties:
                              key:
                                description: The key of the secret to select from.  Must
                                  be a valid secret key.
                                type: string
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                              optional:
                                description: Specify whether the Secret or its key
                                  must be defined
                                type: boolean
                            required:
                            - key
                            type: object
                            x-kubernetes-map-type: atomic
                          value:
                            type: string
                        type: object
                    required:
                    - mechanism
                    type: object
                  tls:
                    description: KafkaTLSSpec configures broker TLS and optional client
                      authentication.
                    properties:
                      ca:
                        description: SecretKeySelector selects a key of a Secret.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      certificate:
                        description: SecretKeySelector selects a key of a Secret.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      privateKey:
                        description: SecretKeySelector selects a key of a Secret.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      serverName:
                        type: string
                    type: object
                required:
                - bootstrapServers
                type: object
              container:
                type: string
              desiredState:
                description: DesiredState is the operator-requested state of a KafkaSplit.
                enum:
                - Enabled
                - Disabled
                type: string
              shadows:
                description: KafkaShadowSpec configures shadow resource ownership.
                properties:
                  managed:
                    description: KafkaManagedShadows configures controller-created
                      topics.
                    properties:
                      configs:
                        additionalProperties:
                          type: string
                        type: object
                      replicationFactor:
                        format: int32
                        maximum: 32767
                        minimum: 1
                        type: integer
                    type: object
                  mode:
                    description: ShadowMode controls who provisions Kafka shadow topics.
                    enum:
                    - Managed
                    - Preprovisioned
                    type: string
                  preprovisioned:
                    description: |-
                      KafkaPreprovisionedShadows lists operator-owned application and session
                      resources.
                    properties:
                      applicationGroup:
                        type: string
                      applicationTopics:
                        additionalProperties:
                          type: string
                        type: object
                      sessions:
                        items:
                          description: KafkaSessionSlot is one reusable preprovisioned
                            set of session topics.
                          properties:
                            group:
                              type: string
                            name:
                              type: string
                            topics:
                              additionalProperties:
                                type: string
                              type: object
                          required:
                          - group
                          - name
                          - topics
                          type: object
                        type: array
                    required:
                    - applicationGroup
                    - applicationTopics
                    - sessions
                    type: object
                required:
                - mode
                type: object
              source:
                description: KafkaSourceSpec identifies the application subscription
                  being split.
                properties:
                  group:
                    type: string
                  offsetReset:
                    enum:
                    - earliest
                    - latest
                    type: string
                  topics:
                    items:
                      type: string
                    minItems: 1
                    type: array
                required:
                - group
                - offsetReset
                - topics
                type: object
              splitter:
                description: KafkaSplitterSpec controls data-plane capacity.
                properties:
                  batchSize:
                    default: 100
                    format: int32
                    minimum: 1
                    type: integer
                  replicas:
                    default: 1
                    format: int32
                    minimum: 1
                    type: integer
                type: object
              workloadSelector:
                description: |-
                  A label selector is a label query over a set of resources. The result of matchLabels and
                  matchExpressions are ANDed. An empty label selector matches all objects. A null
                  label selector matches no objects.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
            required:
            - application
            - connection
            - container
            - desiredState
            - shadows
            - source
            - workloadSelector
            type: object
          status:
            description: KafkaSplitStatus records durable reconciliation and broker
              state.
            properties:
              activeGeneration:
                format: int64
                type: integer
              activeSpec:
                description: |-
                  KafkaActiveSpec is the safety-relevant configuration snapshot currently
                  enforced by admission and broker reconciliation.
                properties:
                  application:
                    description: |-
                      KafkaApplicationSpec describes the selected container's configuration
                      adapter.
                    properties:
                      groupEnv:
                        type: string
                      isolationLevelEnv:
                        type: string
                      shadowCredentials:
                        additionalProperties:
                          description: ValueSource is a literal or one key in a Secret.
                          properties:
                            secretKeyRef:
                              description: SecretKeySelector selects a key of a Secret.
                              properties:
                                key:
                                  description: The key of the secret to select from.  Must
                                    be a valid secret key.
                                  type: string
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                                optional:
                                  description: Specify whether the Secret or its key
                                    must be defined
                                  type: boolean
                              required:
                              - key
                              type: object
                              x-kubernetes-map-type: atomic
                            value:
                              type: string
                          type: object
                        type: object
                      topicBindings:
                        items:
                          description: KafkaTopicEnvBinding maps one source topic
                            to an application variable.
                          properties:
                            env:
                              type: string
                            source:
                              type: string
                          required:
                          - env
                          - source
                          type: object
                        type: array
                      topicEnv:
                        type: string
                      topicSeparator:
                        type: string
                      transactionalIDEnv:
                        type: string
                    required:
                    - groupEnv
                    - isolationLevelEnv
                    type: object
                  connection:
                    description: KafkaConnectionSpec contains provider-only broker
                      connection settings.
                    properties:
                      bootstrapServers:
                        items:
                          type: string
                        minItems: 1
                        type: array
                      sasl:
                        description: KafkaSASLSpec configures one Kafka SASL mechanism.
                        properties:
                          aws:
                            description: KafkaAWSSpec configures AWS MSK IAM authentication.
                            properties:
                              region:
                                type: string
                            required:
                            - region
                            type: object
                          kerberos:
                            description: KafkaKerberosSpec configures SASL/GSSAPI.
                            properties:
                              config:
                                description: SecretKeySelector selects a key of a
                                  Secret.
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              keytab:
                                description: SecretKeySelector selects a key of a
                                  Secret.
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              password:
                                description: ValueSource is a literal or one key in
                                  a Secret.
                                properties:
                                  secretKeyRef:
                                    description: SecretKeySelector selects a key of
                                      a Secret.
                                    properties:
                                      key:
                                        description: The key of the secret to select
                                          from.  Must be a valid secret key.
                                        type: string
                                      name:
                                        default: ""
                                        description: |-
                                          Name of the referent.
                                          This field is effectively required, but due to backwards compatibility is
                                          allowed to be empty. Instances of this type with an empty value here are
                                          almost certainly wrong.
                                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                        type: string
                                      optional:
                                        description: Specify whether the Secret or
                                          its key must be defined
                                        type: boolean
                                    required:
                                    - key
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  value:
                                    type: string
                                type: object
                              realm:
                                type: string
                              serviceName:
                                type: string
                              username:
                                description: ValueSource is a literal or one key in
                                  a Secret.
                                properties:
                                  secretKeyRef:
                                    description: SecretKeySelector selects a key of
                                      a Secret.
                                    properties:
                                      key:
                                        description: The key of the secret to select
                                          from.  Must be a valid secret key.
                                        type: string
                                      name:
                                        default: ""
                                        description: |-
                                          Name of the referent.
                                          This field is effectively required, but due to backwards compatibility is
                                          allowed to be empty. Instances of this type with an empty value here are
                                          almost certainly wrong.
                                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                        type: string
                                      optional:
                                        description: Specify whether the Secret or
                                          its key must be defined
                                        type: boolean
                                    required:
                                    - key
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  value:
                                    type: string
                                type: object
                            required:
                            - config
                            - realm
                            - serviceName
                            - username
                            type: object
                          mechanism:
                            enum:
                            - PLAIN
                            - SCRAM-SHA-256
                            - SCRAM-SHA-512
                            - OAUTHBEARER
                            - GSSAPI
                            - AWS_MSK_IAM
                            type: string
                          oauth:
                            description: KafkaOAuthSpec configures an OAUTHBEARER
                              client-credentials flow.
                            properties:
                              clientID:
                                description: ValueSource is a literal or one key in
                                  a Secret.
                                properties:
                                  secretKeyRef:
                                    description: SecretKeySelector selects a key of
                                      a Secret.
                                    properties:
                                      key:
                                        description: The key of the secret to select
                                          from.  Must be a valid secret key.
                                        type: string
                                      name:
                                        default: ""
                                        description: |-
                                          Name of the referent.
                                          This field is effectively required, but due to backwards compatibility is
                                          allowed to be empty. Instances of this type with an empty value here are
                                          almost certainly wrong.
                                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                        type: string
                                      optional:
                                        description: Specify whether the Secret or
                                          its key must be defined
                                        type: boolean
                                    required:
                                    - key
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  value:
                                    type: string
                                type: object
                              clientSecret:
                                description: ValueSource is a literal or one key in
                                  a Secret.
                                properties:
                                  secretKeyRef:
                                    description: SecretKeySelector selects a key of
                                      a Secret.
                                    properties:
                                      key:
                                        description: The key of the secret to select
                                          from.  Must be a valid secret key.
                                        type: string
                                      name:
                                        default: ""
                                        description: |-
                                          Name of the referent.
                                          This field is effectively required, but due to backwards compatibility is
                                          allowed to be empty. Instances of this type with an empty value here are
                                          almost certainly wrong.
                                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                        type: string
                                      optional:
                                        description: Specify whether the Secret or
                                          its key must be defined
                                        type: boolean
                                    required:
                                    - key
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  value:
                                    type: string
                                type: object
                              scopes:
                                items:
                                  type: string
                                type: array
                              tokenURL:
                                type: string
                            required:
                            - clientID
                            - clientSecret
                            - tokenURL
                            type: object
                          password:
                            description: ValueSource is a literal or one key in a
                              Secret.
                            properties:
                              secretKeyRef:
                                description: SecretKeySelector selects a key of a
                                  Secret.
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              value:
                                type: string
                            type: object
                          username:
                            description: ValueSource is a literal or one key in a
                              Secret.
                            properties:
                              secretKeyRef:
                                description: SecretKeySelector selects a key of a
                                  Secret.
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              value:
                                type: string
                            type: object
                        required:
                        - mechanism
                        type: object
                      tls:
                        description: KafkaTLSSpec configures broker TLS and optional
                          client authentication.
                        properties:
                          ca:
                            description: SecretKeySelector selects a key of a Secret.
                            properties:
                              key:
                                description: The key of the secret to select from.  Must
                                  be a valid secret key.
                                type: string
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                              optional:
                                description: Specify whether the Secret or its key
                                  must be defined
                                type: boolean
                            required:
                            - key
                            type: object
                            x-kubernetes-map-type: atomic
                          certificate:
                            description: SecretKeySelector selects a key of a Secret.
                            properties:
                              key:
                                description: The key of the secret to select from.  Must
                                  be a valid secret key.
                                type: string
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                              optional:
                                description: Specify whether the Secret or its key
                                  must be defined
                                type: boolean
                            required:
                            - key
                            type: object
                            x-kubernetes-map-type: atomic
                          privateKey:
                            description: SecretKeySelector selects a key of a Secret.
                            properties:
                              key:
                                description: The key of the secret to select from.  Must
                                  be a valid secret key.
                                type: string
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                              optional:
                                description: Specify whether the Secret or its key
                                  must be defined
                                type: boolean
                            required:
                            - key
                            type: object
                            x-kubernetes-map-type: atomic
                          serverName:
                            type: string
                        type: object
                    required:
                    - bootstrapServers
                    type: object
                  container:
                    type: string
                  shadows:
                    description: KafkaShadowSpec configures shadow resource ownership.
                    properties:
                      managed:
                        description: KafkaManagedShadows configures controller-created
                          topics.
                        properties:
                          configs:
                            additionalProperties:
                              type: string
                            type: object
                          replicationFactor:
                            format: int32
                            maximum: 32767
                            minimum: 1
                            type: integer
                        type: object
                      mode:
                        description: ShadowMode controls who provisions Kafka shadow
                          topics.
                        enum:
                        - Managed
                        - Preprovisioned
                        type: string
                      preprovisioned:
                        description: |-
                          KafkaPreprovisionedShadows lists operator-owned application and session
                          resources.
                        properties:
                          applicationGroup:
                            type: string
                          applicationTopics:
                            additionalProperties:
                              type: string
                            type: object
                          sessions:
                            items:
                              description: KafkaSessionSlot is one reusable preprovisioned
                                set of session topics.
                              properties:
                                group:
                                  type: string
                                name:
                                  type: string
                                topics:
                                  additionalProperties:
                                    type: string
                                  type: object
                              required:
                              - group
                              - name
                              - topics
                              type: object
                            type: array
                        required:
                        - applicationGroup
                        - applicationTopics
                        - sessions
                        type: object
                    required:
                    - mode
                    type: object
                  source:
                    description: KafkaSourceSpec identifies the application subscription
                      being split.
                    properties:
                      group:
                        type: string
                      offsetReset:
                        enum:
                        - earliest
                        - latest
                        type: string
                      topics:
                        items:
                          type: string
                        minItems: 1
                        type: array
                    required:
                    - group
                    - offsetReset
                    - topics
                    type: object
                  splitter:
                    description: KafkaSplitterSpec controls data-plane capacity.
                    properties:
                      batchSize:
                        default: 100
                        format: int32
                        minimum: 1
                        type: integer
                      replicas:
                        default: 1
                        format: int32
                        minimum: 1
                        type: integer
                    type: object
                required:
                - application
                - connection
                - container
                - shadows
                - source
                - splitter
                type: object
              admissionMode:
                description: KafkaAdmissionMode controls how replacement Pods are
                  admitted.
                enum:
                - Normal
                - Shadow
                type: string
              applicationEnv:
                additionalProperties:
                  type: string
                type: object
              applicationGroup:
                type: string
              applicationLag:
                format: int64
                type: integer
              applicationTopics:
                additionalProperties:
                  type: string
                type: object
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              members:
                items:
                  description: KafkaSplitterMemberStatus describes one acknowledged
                    StatefulSet member.
                  properties:
                    generation:
                      format: int64
                      type: integer
                    healthy:
                      type: boolean
                    lastSeen:
                      format: date-time
                      type: string
                    name:
                      type: string
                  required:
                  - generation
                  - healthy
                  - lastSeen
                  - name
                  type: object
                type: array
              observedGeneration:
                format: int64
                type: integer
              phase:
                description: SplitPhase is the reconciliation phase of a KafkaSplit.
                type: string
              resources:
                items:
                  description: KafkaResourceStatus is one broker resource owned or
                    leased by a split.
                  properties:
                    kind:
                      type: string
                    managed:
                      type: boolean
                    name:
                      type: string
                    routeName:
                      type: string
                    source:
                      type: string
                    topicID:
                      type: string
                  required:
                  - kind
                  - managed
                  - name
                  type: object
                type: array
              routeGeneration:
                format: int64
                type: integer
              sourceTopics:
                items:
                  description: KafkaTopicStatus identifies one source topic incarnation.
                  properties:
                    name:
                      type: string
                    partitions:
                      format: int32
                      type: integer
                    topicID:
                      type: string
                  required:
                  - name
                  - partitions
                  - topicID
                  type: object
                type: array
              splitterName:
                type: string
              transactionalIDPrefix:
                type: string
              workloads:
                items:
                  description: WorkloadReference is an immutable member of the active
                    workload snapshot.
                  properties:
                    apiVersion:
                      type: string
                    kind:
                      type: string
                    name:
                      type: string
                    uid:
                      description: |-
                        UID is a type that holds unique ID values, including UUIDs.  Because we
                        don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                        intent and helps make sure that UIDs and names do not get conflated.
                      type: string
                  required:
                  - apiVersion
                  - kind
                  - name
                  - uid
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
